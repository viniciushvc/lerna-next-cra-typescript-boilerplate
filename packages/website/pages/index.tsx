import Head from "next/head";

import { Button } from "components";

const Home = () => (
  <div className="container">
    <Head>
      <title>Home</title>
    </Head>

    <Button>Test</Button>
  </div>
);

export default Home;
